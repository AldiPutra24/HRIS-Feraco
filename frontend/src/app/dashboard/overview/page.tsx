import { OverviewDashboard } from '@/features/hris-dashboard/overview-dashboard';

export const metadata = {
  title: 'Dashboard'
};

export default function OverviewPage() {
  return <OverviewDashboard />;
}
